// Configuration settings
var config = {}


config.configureImap = false	// If false, the client doesn't present the user with an IMAP configuration panel. This means that server-side defaults are automatically used.
